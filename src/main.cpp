#include <iostream>
#include <filesystem>

// Generated by CMake
#include "config.hpp"

#ifdef USE_MATH
    #include "math/my_math.hpp"
#else
    #include <cmath>
#endif

int main()
{
    std::string input;
    std::cout << "Fala, galera!" << std::endl;
    std::cin >> input;

    std::filesystem::path p{"/"};
    const double value = std::stod(input);
    std::cout << "Número fornecido: " << value << std::endl;
    
    #ifdef USE_MATH
    double sqroot = util::math::mySqrt(value);
    std::cout << "Math do usuário" << std::endl;
    #else
    double sqroot = sqrt(value);
    std::cout << "Math std" << std::endl;
    #endif
        
    std::cout << "Sua raiz aproximada: " << sqroot << std::endl;
    std::cout << "Versão brasileira " << VERSION_MAJOR << "." << VERSION_MINOR << std::endl;
}